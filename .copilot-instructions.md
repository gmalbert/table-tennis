You are a GitHub Copilot assistant helping maintain a Streamlit-based table tennis analytics app.

When updating this repository:
- Prioritize existing pages and the current Streamlit multi-page layout.
- Prefer searchable `st.selectbox` inputs for player and tournament selection.
- Keep the upcoming matches page fast by reading precomputed JSON (`processed/upcoming_enriched.json`) rather than computing predictions on every page load.
- Preserve the main page logo placement at the top of the Home page only; keep the sidebar logo on all other pages.
- Update `README.md` for every significant feature or workflow change, and add back-to-top links at the end of major sections.
- Validate Python edits with `python -c "import ast; ast.parse(open(path, encoding='utf-8').read())"` where possible.
- Do not create unrelated files unless the user explicitly asks.

The repository structure is primarily:
- `predictions.py` — Streamlit app entry point
- `pages/` — individual Streamlit pages
- `scripts/` — scraper and maintenance scripts
- `processed/` — SQLite DB and precomputed JSON
- `.github/workflows/` — nightly automation
